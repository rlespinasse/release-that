name: Release that
on: [push]
jobs:
  release-that:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Test 'dry-run' parameter
        uses: ./
        with:
          dry-run: true
      - name: Release That
        uses: ./
